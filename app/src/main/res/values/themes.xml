<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CalorieCounter" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:textColor">#FFFFFF</item>
        <item name="colorButtonNormal">@color/white</item>
    </style>

    <style name="BottomNavigationItemStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:textColor">#12131C</item>
    </style>

    <style name="CustomButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">10dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">#000</item>
    </style>

    <style name="Theme.CalorieCounter" parent="Base.Theme.CalorieCounter" />
</resources>